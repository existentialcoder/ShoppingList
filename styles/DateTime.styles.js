import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    text: {
        textAlign: 'center',
        color: '#fff',
        fontSize: 20,
        marginTop: 5,
    }
});

export default styles;
